name: Publish Production Helm Charts
on:
  push:
    tags:
      - '*'

jobs:
  call-workflow-passing-data:
    uses: GlueOps/github-workflows/.github/workflows/publish-helm-chart.yml@536dd7ba5e206a5a9ba4c01b0b5fc15aa5f5690a # main
    secrets: inherit
    with:
      GLUEOPS_ENV: "production"
      HELM_CHART_REPO_NAME: "platform"